import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-ng-auth-ui-tutorial',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
